// Array is defined as a collection of hetrogenous elements.

function Jyotiraditya() {

     let Fruits = ["Apple", "Banana", "Mango", "Grapes",  ]

    // document.write(Fruits)
    // document.write(Fruits.length)
    // document.write(Fruits[3]) to access with index number

    // POP, PUSH, SHIFT, UNSHIFT

    // Fruits.pop() //(removes last element)
    // Fruits.push("orange")  //add element from back
    // Fruits.shift()  //removes first element
    // Fruits.unshift("Pineapple") //add element from front

     //console.log(Fruits);

    //JOIN(seperator) array se string me convert krne ke liye

// ----------- (bich me add krne ke liye slice and bich se hatane ke liye splice use krte hai)---------

    //let Fruits = ["Apple",  "Mango", "Grapes", "Orange" ]

    //console.log(Fruits.slice(0,-1)); (-1 means last se ek element hatana )

    //console.log(Fruits.slice (1,3));

    //Fruits.splice(1,0,"papaya") // 1st index se 0 element hatana hai aur papaya add krna hai

    //console.log(Fruits.reverse());

    // --------------2------------

    // let Fruits = ["Apple",  "Mango", "Grapes", "Orange" ]

    // let Fruit = ["RAM",  "SHYAM", "GOPAL",  ]

    // let final = Fruits.concat(Fruit)

    // console.log(final);

    // ------------3-----------

    // let text = "Jyotiraditya"

    // console.log(text.split(' ').reverse().join(''));

    

}